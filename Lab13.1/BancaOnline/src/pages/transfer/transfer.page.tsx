import { AppLayout } from "@/layouts";
import React from "react";

export const TransferPage: React.FC = () => {
    return (
        <AppLayout>
            <div>Trasnfer Page</div>
        </AppLayout>
    );
};